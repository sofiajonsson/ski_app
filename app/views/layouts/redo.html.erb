<%= button_to 'Listings', listings_path %> <% if session[:user_id] %> <%=button_to 'Logout', "/logout", {method: :delete} %> <% else %> <%= button_to 'Login', login_path(@user) %><% end %><%= button_to 'Sign Up', new_user_path %>
