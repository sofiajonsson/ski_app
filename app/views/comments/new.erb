<h1> Create a New Comment </h1>
<%= form_for :comment, url: '/comments' do |c| %>
<h2><%= c.label :message %></h2>
<%= c.text_area :message %>
<h2><%= c.label :listing_id %></h2>
<%= c.collection_select :listing_id ,Listing.all, :id, :title %>
<h2><%=c.submit %></h2>
<% end %>
